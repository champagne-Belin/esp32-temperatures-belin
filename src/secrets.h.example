#ifndef SECRETS_H
#define SECRETS_H

// ============================================================
// Fichier partagé par tous les modules (cave, habillage,
// pressoir, maison). Copier ce fichier dans le dossier de CHAQUE
// module avant de compiler (Arduino IDE exige que tous les fichiers
// soient dans le dossier du sketch).
// NE JAMAIS COMMITER CE FICHIER (voir .gitignore).
// ============================================================

struct Reseau { const char* ssid; const char* password; };

Reseau reseaux[] = {
  { "TON_SSID_1", "TON_MDP_1" },
  { "TON_SSID_2", "TON_MDP_2" },
  { "TON_SSID_3", "TON_MDP_3" }
};
const int NB_RESEAUX = 3;

#define SCRIPT_URL "TON_URL_APPS_SCRIPT_EXEC_ICI"

#endif
